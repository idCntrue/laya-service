# Runtime dependencies. Mirrors [project].dependencies in pyproject.toml --
# pyproject.toml is the source of truth; this file exists for deployments that
# install from a plain requirements file (Docker, some CI systems).
#
# IMPORTANT: install a CPU-only torch FIRST on machines without a GPU, or pip
# will pull the CUDA build (~2.5 GB) and its nvidia-* wheels:
#
#   pip install torch==2.9.1+cpu --index-url https://download.pytorch.org/whl/cpu
#
# `make install` does this automatically. See README > Installation.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
huggingface_hub>=0.22

# The decision model. Pulls torch>=2.0, transformers>=4.48, safetensors, numpy.
laya>=0.3.5
